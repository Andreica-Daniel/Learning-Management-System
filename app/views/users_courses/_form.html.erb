<%= form_with(model: users_course) do |form| %>
  <% if users_course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(users_course.errors.count, "error") %> prohibited this users_course from being saved:</h2>

      <ul>
        <% users_course.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field" data-controller="select">
    <%= form.label :user_id %>
    <%= form.select :user_id, User.pluck(:id), { include_blank: false }, class: "form-input", data: { target: "select.input" } %>
  </div>

  <div class="field" data-controller="select">
    <%= form.label :course_id %>
    <%= form.select :course_id, Course.pluck(:id), { include_blank: false }, class: "form-input", data: { target: "select.input" } %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
